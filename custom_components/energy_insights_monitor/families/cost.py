"""Family: cost.

Replaces the generator fragment(s) for 'cost'. TODO: implement build().
Cumulative/cycle sensors here should subclass the Lean core (see ..lean).
"""


def build(hass, device):
    """Return the cost entities for a resolved device. TODO."""
    return []
