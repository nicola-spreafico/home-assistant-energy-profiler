"""Family: cycles.

Replaces the generator fragment(s) for 'cycles'. TODO: implement build().
Cumulative/cycle sensors here should subclass the Lean core (see ..lean).
"""


def build(hass, device):
    """Return the cycles entities for a resolved device. TODO."""
    return []
